<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="dev.ltag.stone_payments">
    <!-- <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:host="pay-response" android:scheme="stonepayments" />
        </intent>
    </queries> -->
</manifest>